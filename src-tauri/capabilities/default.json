{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Unified capability for all local windows",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "core:path:default",
    "fs:default",
    "opener:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}

# Media Engine 开发方案

## 1. 目标与硬性约束

- 产品定位：专业级媒体播放器与处理器，支持 4K/HDR、主流封装格式与主流编码格式。
- 分发目标：开箱即用，用户无需额外安装 FFmpeg 或系统插件。
- 构建策略：FFmpeg 采用静态链接，禁止依赖 Sidecar 外部二进制。
- 平台范围：第一阶段支持 macOS、Windows；CI 必须覆盖双平台构建。
- 架构原则：高内聚、低耦合，底层引擎与前端交互通过稳定协议层隔离。

后端解码首帧 + 连续帧事件 + 前端 canvas 渲染
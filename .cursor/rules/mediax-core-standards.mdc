---
description: MediaX 全局开发与交付规范
alwaysApply: true
---

# MediaX Core Standards

- 默认直接落地实现，除非用户明确只要方案/讨论。
- 代码保持简洁紧凑，避免重复逻辑；优先重构复用，再新增分支。
- 仅添加必要注释：解释复杂约束、边界或设计意图，不写显而易见注释。
- 不擅自扩大改动面；优先最小可行改动并保持行为兼容。
- 每次改动后执行最小必要验证（类型检查/编译/lint）；失败时先修复再交付。
- 输出结果必须包含：改了什么、在哪些文件、如何验证。
- 涉及密钥/凭据时：不提交、不打印明文；仅通过 Secret 或本地安全存储管理。
